import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{doc,getDoc,getFirestore}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';import{levelBadge}from'./level-badges.js?v=2';import{levelFromXp,levelLabel}from'./level-system.js?v=2';
const PREF_PREFIX='shadrat-profile-v2:';
const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),root=document.querySelector('#public-profile'),uid=new URLSearchParams(location.search).get('uid');const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function prefsOf(profile={}){const raw=String(profile.avatarKey||'');if(!raw.startsWith(PREF_PREFIX))return{};try{return JSON.parse(raw.slice(PREF_PREFIX.length))||{}}catch{return{}}}
function avatarMarkup(profile={}){const prefs=prefsOf(profile),avatar=typeof prefs.avatarDataUrl==='string'&&prefs.avatarDataUrl.startsWith('data:image/')?prefs.avatarDataUrl:'',frame=/^(blue|glow|silver|gold|diamond|elite)$/.test(String(prefs.frame||''))?prefs.frame:'none',initial=esc((profile.username||'ط').slice(0,1).toUpperCase());return`<div class="public-avatar${frame!=='none'?` frame-${frame}`:''}">${avatar?`<img src="${esc(avatar)}" alt="الصورة الشخصية">`:initial}</div>`}
if(!uid){root.innerHTML='<div class="notice">رابط الملف غير صحيح.</div>'}else{try{const snap=await getDoc(doc(db,'publicProfiles',uid));if(!snap.exists()||snap.data().visible!==true){root.innerHTML='<div class="notice">هذا الملف غير متاح للعامة.</div>'}else{const p=snap.data(),xp=Math.max(0,Number(p.xp)||0),level=levelFromXp(xp),label=levelLabel(level);root.innerHTML=`<div class="public-head">${avatarMarkup(p)}<div><div class="public-name"><h1 style="margin:0">@${esc(p.username||'student')}</h1>${levelBadge(level,34)}</div><p class="muted">طالب في شذرات للمنح</p></div></div><div class="public-stats"><div class="public-stat"><b>Level ${label}</b><small>المستوى</small></div><div class="public-stat"><b>${xp.toLocaleString('ar')}</b><small>XP</small></div><div class="public-stat"><b>${Array.isArray(p.badges)?p.badges.length:0}</b><small>الشارات</small></div></div><p class="privacy-note">يعرض هذا الملف المعلومات العامة فقط. البريد ورقم الجوال والطلبات والرسائل وبيانات الحساب الخاصة لا تظهر للطلاب الآخرين.</p>`}}catch(e){console.error(e);root.innerHTML='<div class="notice">تعذر تحميل الملف الآن.</div>'}}
