import { getApp, getApps, initializeApp } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import { getAuth, onAuthStateChanged } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js';
import { addDoc, collection, doc, getDoc, getDocs, getFirestore, limit, onSnapshot, orderBy, query, serverTimestamp, where } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import { firebaseConfig } from './firebase-config.js';
import { scholarshipCatalog, mergeScholarships } from './scholarship-catalog.js';
const app = getApps().length ? getApp() : initializeApp(firebaseConfig);
const auth = getAuth(app); const db = getFirestore(app);
const status=document.querySelector('#community-status'),postsRoot=document.querySelector('#community-posts'),composerButton=document.querySelector('[data-requires-account]'),loginDialog=document.querySelector('#login-dialog'),postDialog=document.querySelector('#post-dialog'),postForm=document.querySelector('#community-post-form'),postMessage=document.querySelector('#post-form-message');
let currentUser=null,currentProfile=null; const typeLabels={question:'سؤال',experience:'تجربة طالب',info:'معلومة',guide:'دليل',update:'تحديث',warning:'تحذير',accepted:'نتيجة قبول'};let scholarships=scholarshipCatalog.filter(i=>i.publishStatus==='published');let scholarshipLabels=Object.fromEntries(scholarships.map(i=>[i.slug,i.title]));
const personalData=/(?:\+?\d[\d\s-]{7,}|[\w.+-]+@[\w.-]+\.[A-Za-z]{2,})/;const open=e=>{e?.classList.add('is-open');e?.setAttribute('aria-hidden','false');document.body.classList.add('menu-open')},close=e=>{e?.classList.remove('is-open');e?.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open')};
function renderScholarshipChoices(){scholarshipLabels=Object.fromEntries(scholarships.map(i=>[i.slug,i.title]));const f=document.querySelector('#scholarship-filter'),c=document.querySelector('#new-post-scholarship');if(f)f.innerHTML='<option value="">كل المنح</option>'+scholarships.map(i=>`<option value="${i.slug}">${i.title}</option>`).join('');if(c)c.innerHTML='<option value="">غير محددة</option>'+scholarships.map(i=>`<option value="${i.slug}">${i.title}</option>`).join('')}
renderScholarshipChoices();getDocs(collection(db,'scholarships')).then(s=>{scholarships=mergeScholarships(s.docs.map(i=>i.data())).filter(i=>i.publishStatus==='published');renderScholarshipChoices()}).catch(()=>{});
document.querySelectorAll('[data-post-close]').forEach(b=>b.addEventListener('click',()=>close(postDialog)));composerButton?.addEventListener('click',e=>{e.stopImmediatePropagation();currentUser?open(postDialog):open(loginDialog)});
function renderEmpty(){status.hidden=false;status.querySelector('h2').textContent='لا توجد مشاركات حتى الآن';status.querySelector('p').textContent='كن أول من يطرح سؤالًا مفيدًا بعد تسجيل الدخول.';postsRoot.replaceChildren()}
function postCard(id,post){const a=document.createElement('article');a.className='community-post';a.innerHTML=`<div class="post-head"><div class="post-author"><span class="avatar">${(post.authorUsername||'ش').slice(0,1).toUpperCase()}</span><div><b>@${post.authorUsername||'طالب_شذرات'}</b><small>المستوى ${Number(post.authorLevel)||1}</small></div></div><span class="post-badge">${typeLabels[post.type]||'مشاركة'}</span></div>${post.title?`<h2>${post.title}</h2>`:''}<p class="post-body"></p><div class="post-meta"></div><div class="post-actions"><button type="button">مفيد ${Number(post.helpfulCount)||0}</button><button type="button">التعليقات ${Number(post.commentCount)||0}</button><button type="button">حفظ</button><button type="button">إبلاغ</button></div>`;a.querySelector('.post-body').textContent=post.body||'';const meta=a.querySelector('.post-meta');if(post.scholarshipSlug){const s=document.createElement('span');s.textContent=scholarshipLabels[post.scholarshipSlug]||post.scholarshipSlug;meta.append(s)}return a}
const feedQuery=query(collection(db,'communityPosts'),where('status','==','published'),orderBy('createdAt','desc'),limit(10));onSnapshot(feedQuery,s=>{if(s.empty)return renderEmpty();status.hidden=true;postsRoot.replaceChildren(...s.docs.map(i=>postCard(i.id,i.data())))},()=>{status.hidden=false;status.querySelector('h2').textContent='تعذّر تحميل المجتمع'});
onAuthStateChanged(auth,async user=>{currentUser=user;currentProfile=null;if(user)try{currentProfile=(await getDoc(doc(db,'users',user.uid))).data()||null}catch{}if(composerButton)composerButton.textContent=user?'اكتب سؤالًا أو تجربة...':'سجّل دخولك للمشاركة'});
postForm?.addEventListener('submit',async e=>{e.preventDefault();if(!currentUser)return open(loginDialog);const body=document.querySelector('#new-post-body').value.trim();if(body.length<10){postMessage.textContent='اكتب 10 أحرف على الأقل.';return}if(personalData.test(body)){postMessage.textContent='احذف رقم الهاتف أو البريد من المنشور.';return}const submit=postForm.querySelector('[type="submit"]');submit.disabled=true;try{await addDoc(collection(db,'communityPosts'),{authorId:currentUser.uid,authorUsername:currentProfile?.username||`student_${currentUser.uid.slice(0,6)}`,authorLevel:Number(currentProfile?.level)||1,title:document.querySelector('#new-post-title').value.trim(),body,type:document.querySelector('#new-post-type').value,scholarshipSlug:document.querySelector('#new-post-scholarship').value,status:'published',helpfulCount:0,commentCount:0,saveCount:0,createdAt:serverTimestamp(),updatedAt:serverTimestamp(),deletedAt:null});postForm.reset();close(postDialog)}catch(err){postMessage.textContent='تعذر النشر الآن.'}finally{submit.disabled=false}});
