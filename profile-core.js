import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js';
import{doc,getDoc,getFirestore}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import{firebaseConfig}from'./firebase-config.js';
import{PROFILE_BADGES,PROFILE_FRAMES,PROFILE_NAME_COLORS,PROFILE_THEMES,levelFromXp}from'./level-system.js?v=4';

const OWNER_EMAIL='shadrat.almn7@gmail.com',AUTH_SESSION_KEY='shadrat_auth_session',CACHE_PREFIX='shadrat-profile-cache-v3:',PREF_PREFIX='shadrat-profile-v2:';
const app=getApps().length?getApp():initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app),tasks=new Map();
function sessionUid(){try{return JSON.parse(sessionStorage.getItem(AUTH_SESSION_KEY)||'null')?.uid||''}catch{return''}}
function readCache(uid){try{return JSON.parse(localStorage.getItem(`${CACHE_PREFIX}${uid}`)||'null')}catch{return null}}
function writeCache(user,data){try{localStorage.setItem(`${CACHE_PREFIX}${user.uid}`,JSON.stringify({uid:user.uid,email:user.email||data.email||'',data,at:Date.now()}))}catch{}}
function rawPrefs(data){const raw=String(data?.avatarKey||'');if(!raw.startsWith(PREF_PREFIX))return{};try{return JSON.parse(raw.slice(PREF_PREFIX.length))||{}}catch{return{}}}
function unlocked(list,id,level){const item=list.find(x=>x.id===id);return!!item&&level>=item.level}
function completionPercent(data={},email=''){const values=[data.fullName||data.displayName,data.username,email||data.email,data.age,data.nationality,data.currentCountry||data.country,data.city,data.studyLevel,data.contactMethod,data.contactValue];return Math.round(values.filter(v=>v!==null&&v!==undefined&&String(v).trim()!=='').length/values.length*100)}

function applyAppearance(data={}){
 const level=levelFromXp(data.xp||0),prefs=rawPrefs(data),cover=document.querySelector('.student-cover'),avatar=document.querySelector('[data-student-avatar]'),name=document.querySelector('[data-student-name]'),studentName=document.querySelector('.student-name');
 if(!cover||!avatar||!name)return;
 const theme=unlocked(PROFILE_THEMES,prefs.theme,level)?prefs.theme:'default',frame=unlocked(PROFILE_FRAMES,prefs.frame,level)?prefs.frame:'none',nameColor=unlocked(PROFILE_NAME_COLORS,prefs.nameColor,level)?prefs.nameColor:'white',customColor=level>=20&&/^#[0-9a-f]{6}$/i.test(prefs.customNameColor||'')?prefs.customNameColor:'',accent=level>=16&&/^#[0-9a-f]{6}$/i.test(prefs.accent||'')?prefs.accent:'#2563eb',avatarData=typeof prefs.avatarDataUrl==='string'&&prefs.avatarDataUrl.startsWith('data:image/')?prefs.avatarDataUrl:'';
 [...cover.classList].filter(c=>c.startsWith('theme-')).forEach(c=>cover.classList.remove(c));
 if(theme!=='default')cover.classList.add(`theme-${theme}`);cover.style.setProperty('--student-accent',accent);
 avatar.className='student-avatar'+(frame!=='none'?` frame-${frame}`:'');if(avatarData)avatar.innerHTML=`<img src="${avatarData}" alt="الصورة الشخصية">`;else avatar.textContent=(String(data.fullName||data.displayName||'ط').trim()[0]||'ط').toUpperCase();
 const color=customColor||PROFILE_NAME_COLORS.find(x=>x.id===nameColor)?.value||'#fff';name.style.color=color;studentName?.classList.toggle('name-effect',level>=13&&!!prefs.nameEffect);
 studentName?.querySelector('.student-profile-badges')?.remove();let ids=level>=18&&Array.isArray(prefs.showcaseBadges)&&prefs.showcaseBadges.length?prefs.showcaseBadges:[prefs.badge||'student'];ids=[...new Set(ids)].filter(id=>unlocked(PROFILE_BADGES,id,level)).slice(0,level>=18?3:1);if(studentName&&ids.length){const box=document.createElement('div');box.className='student-profile-badges';box.innerHTML=ids.map(id=>{const b=PROFILE_BADGES.find(x=>x.id===id);return b?`<span class="student-profile-badge">${b.icon} ${b.label}</span>`:''}).join('');studentName.appendChild(box)}
}
function renderDashboardTask(){const title=document.querySelector('#profile-next-title'),copy=document.querySelector('#profile-next-text'),button=document.querySelector('#profile-next-action');if(!title||!copy||!button)return;const task=[...tasks.values()].sort((a,b)=>(b.priority||0)-(a.priority||0))[0]||{title:'استكشف الفرص المناسبة لك',text:'ابدأ من المنح المفتوحة واحفظ ما يناسبك للرجوع إليه بسهولة.',label:'عرض المنح',href:'scholarships.html'};title.textContent=task.title;copy.textContent=task.text;button.textContent=task.label||'متابعة';button.onclick=()=>{if(task.edit){location.href='profile-settings.html';return}if(task.href){location.href=task.href;return}if(task.tab)window.ShadratProfileNav?.open(task.tab,{sub:task.sub||''})}}
function setTask(source,task){if(task)tasks.set(source,task);else tasks.delete(source);renderDashboardTask()}
window.ShadratProfileDashboard={setTask,openTab:id=>window.ShadratProfileNav?.open(id)};
document.addEventListener('click',event=>{const button=event.target.closest?.('[data-profile-edit]');if(!button)return;event.preventDefault();event.stopImmediatePropagation();location.href='profile-settings.html'},true);
function renderProfile(data={},email=''){
 const name=data.fullName||data.displayName||'طالب شذرات',username=data.username||'student',nameEl=document.querySelector('[data-student-name]'),userEl=document.querySelector('[data-student-username]'),avatar=document.querySelector('[data-student-avatar]');if(nameEl)nameEl.textContent=name;if(userEl)userEl.textContent=`@${username}`;if(avatar&&!String(data.avatarKey||'').includes('avatarDataUrl'))avatar.textContent=(name.trim()[0]||'ط').toUpperCase();
 const values={fullName:name,username:`@${username}`,email:email||data.email||'—',phone:data.phone||(data.phoneLast4?`•••• ${data.phoneLast4}`:'غير مضاف'),age:data.age?`${data.age} سنة`:'غير محدد',nationality:data.nationality||'غير محدد',currentCountry:data.currentCountry||data.country||'غير محدد',city:data.city||'غير محدد',studyLevel:data.studyLevel||'غير محدد',contact:[data.contactMethod,data.contactValue].filter(Boolean).join(' — ')||'غير محدد'};for(const[key,value]of Object.entries(values)){const el=document.querySelector(`[data-profile-value="${key}"]`);if(el)el.textContent=value}
 const complete=completionPercent(data,email),bar=document.querySelector('#profile-progress-bar'),label=document.querySelector('#profile-progress-value');if(bar)bar.style.width=`${complete}%`;if(label)label.textContent=`${complete}%`;applyAppearance(data);
 const labels=[['الاسم الكامل',data.fullName||data.displayName],['اسم المستخدم',data.username],['العمر',data.age],['الجنسية',data.nationality],['الدولة الحالية',data.currentCountry||data.country],['المدينة',data.city],['المرحلة الدراسية',data.studyLevel],['وسيلة التواصل',data.contactMethod],['بيانات التواصل',data.contactValue]];const missing=labels.find(x=>!x[1]);if(missing)setTask('profile',{priority:20,title:`أكمل ${missing[0]}`,text:`ملفك مكتمل بنسبة ${complete}٪. أكمل بياناتك من زر القلم.`,label:'تعديل حسابي',edit:true});else setTask('profile',null)
}
function ownerMode(user,data){document.body.dataset.profileMode='owner';const tabs=document.querySelector('.profile-tabs');if(tabs)tabs.innerHTML='<a class="owner-profile-link is-active" href="profile.html">ملف المالك</a><a class="owner-profile-link" href="admin-analytics.html">لوحة الإدارة</a><a class="owner-profile-link" href="admin-users.html">إدارة الطلاب</a><a class="owner-profile-link" href="admin-homepage.html">إدارة المحتوى</a>';document.querySelectorAll('[data-profile-panel="overview"],[data-profile-panel="level"],[data-profile-panel="rewards"],[data-profile-panel="orders"],[data-profile-panel="support"],#missions,#level,#rewards,#orders').forEach(el=>el.remove());const info=document.querySelector('[data-profile-panel="student-info"]');if(info)info.classList.add('is-active');renderProfile({...data,fullName:data.fullName||'OWNER | المالك',username:data.username||'OWNER'},user.email||data.email||'')}
const cachedUid=sessionUid();if(cachedUid){const cached=readCache(cachedUid);if(cached?.data)renderProfile(cached.data,cached.email||'')}renderDashboardTask();
onAuthStateChanged(auth,async user=>{if(!user){location.replace('login.html?next=profile.html');return}try{const snap=await getDoc(doc(db,'users',user.uid)),data=snap.data()||{};writeCache(user,data);if((user.email||'').toLowerCase()===OWNER_EMAIL){ownerMode(user,data);return}renderProfile(data,user.email||data.email||'')}catch(error){console.error('[Shadrat] profile load',error);const cached=readCache(user.uid);if(cached?.data)renderProfile(cached.data,cached.email||user.email||'')}});