import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js';
import{doc,getDoc,getFirestore}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import{firebaseConfig}from'./firebase-config.js';
const OWNER_EMAIL='shadrat.almn7@gmail.com';
const app=getApps().length?getApp():initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app);
const esc=v=>String(v??'');
function selectTab(id){document.querySelectorAll('[data-profile-tab]').forEach(b=>b.classList.toggle('is-active',b.dataset.profileTab===id));document.querySelectorAll('[data-profile-panel]').forEach(p=>p.classList.toggle('is-active',p.dataset.profilePanel===id));}
document.querySelectorAll('[data-profile-tab]').forEach(b=>b.addEventListener('click',()=>selectTab(b.dataset.profileTab)));
onAuthStateChanged(auth,async user=>{if(!user){location.replace('login.html');return}let data={};try{const s=await getDoc(doc(db,'users',user.uid));data=s.data()||{}}catch{}const isOwner=(user.email||'').toLowerCase()===OWNER_EMAIL;const name=isOwner?'OWNER | المالك':(data.fullName||user.displayName||'طالب شذرات'),username=isOwner?'OWNER':(data.username||`student_${user.uid.slice(0,6)}`);document.querySelector('[data-student-name]').textContent=name;document.querySelector('[data-student-username]').textContent=isOwner?'المالك الرئيسي للمنصة':`@${username}`;document.querySelector('[data-student-avatar]').textContent=isOwner?'O':(name.trim()[0]||'ط').toUpperCase();const values={fullName:name,username:isOwner?'OWNER | المالك':`@${username}`,email:user.email||data.email||'—',phone:data.phoneE164||data.phone||data.phoneLast4&&`•••• ${data.phoneLast4}`||'غير مضاف',location:data.location||data.country||'غير محدد',studyLevel:data.studyLevel||'غير محدد'};for(const [key,val] of Object.entries(values)){const el=document.querySelector(`[data-profile-value="${key}"]`);if(el)el.textContent=esc(val)}const fields=[name,user.email,data.location||data.country,data.studyLevel,username],complete=Math.round(fields.filter(Boolean).length/fields.length*100);const bar=document.querySelector('#profile-progress-bar'),label=document.querySelector('#profile-progress-value');if(bar)bar.style.width=`${complete}%`;if(label)label.textContent=`${complete}%`;});
