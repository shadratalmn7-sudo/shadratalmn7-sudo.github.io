import{getApp,getApps,initializeApp}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';import{collection,getDocs,getFirestore,query,where}from'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';import{firebaseConfig}from'./firebase-config.js';import{mergeScholarships}from'./scholarship-catalog.js';if(location.pathname.endsWith('/')||location.pathname.endsWith('/index.html')){document.querySelector('.hero .cta [data-guest-only][href="register.html"]')?.remove();const initialStats=document.querySelectorAll('.stats .stat b');if(initialStats[0])initialStats[0].textContent='—';if(initialStats[1])initialStats[1].textContent='—';if(initialStats[2])initialStats[2].textContent='جارٍ التحديث';if(!document.querySelector('link[data-home-scholarships]')){const l=document.createElement('link');l.rel='stylesheet';l.href='homepage-scholarships.css?v=2';l.dataset.homeScholarships='';document.head.appendChild(l)}const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c])),theme=c=>c==='روسيا'?'ru':c==='المجر'?'hu':c==='تركيا'?'tr':'global';let remote=[];try{const snap=await getDocs(query(collection(db,'scholarships'),where('publishStatus','==','published')));remote=snap.docs.map(d=>({id:d.id,...d.data()}))}catch(e){console.warn('Homepage remote data unavailable; keeping verified catalog.',e)}const items=mergeScholarships(remote).filter(x=>x.publishStatus==='published').sort((a,b)=>(a.sortOrder??999)-(b.sortOrder??999));const featured=items.filter(x=>x.featured).slice(0,3);const chosen=featured.length>=3?featured:items.slice(0,3),cards=document.querySelector('.featured .cards');if(cards&&chosen.length)cards.innerHTML=chosen.map(s=>{const href=`scholarship.html?slug=${encodeURIComponent(s.slug||s.id)}`,levels=(s.studyLevels||[]).slice(0,3).join(' • ');return `<article class="card home-scholarship theme-${theme(s.country)}"><div class="cover"><div class="cover-glow"></div><div><small>${esc(s.country||'دولي')}</small><h3>${esc(s.title||'منحة')}</h3><span>${esc(s.provider||'جهة مانحة')}</span></div></div><div class="body"><div class="pills"><span class="pill">${esc(s.statusLabel||'راجع الحالة')}</span><span class="pill gold">${esc(s.funding||'حسب البرنامج')}</span></div><p>${esc(s.shortDescription||'')}</p>${levels?`<div class="home-levels">${esc(levels)}</div>`:''}<div class="bottom"><div>موعد الإغلاق<br><b>${esc(s.deadline||'حسب الجهة')}</b></div><a class="detail" href="${href}">عرض المنحة</a></div></div></article>`}).join('');const stats=document.querySelectorAll('.stats .stat b'),countries=new Set(items.map(x=>x.country).filter(Boolean));if(stats[0])stats[0].textContent=items.length.toLocaleString('ar');if(stats[1])stats[1].textContent=countries.size.toLocaleString('ar');if(stats[2])stats[2].textContent='محدثة';}