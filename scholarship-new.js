import{scholarshipCatalog}from'./scholarship-catalog.js';
const root=document.getElementById('new-scholarship');const slug=new URLSearchParams(location.search).get('slug');const item=scholarshipCatalog.find(x=>x.slug===slug);const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
if(!item){root.innerHTML='<div class="empty">لم نعثر على هذه المنحة. <a href="scholarships.html">العودة إلى المنح</a></div>';}else{document.title=`${item.title} | شذرات للمنح`;const levels=item.studyLevels||[];const levelSpecific=(arr,l)=>{const named=/بكالوريوس|ماجستير|دكتوراه|ما بعد الدكتوراه|دبلوم|تخصص/;const common=(arr||[]).filter(x=>!named.test(String(x)));const specific=(arr||[]).filter(x=>String(x).includes(l));return [...common,...specific]};const stageFilter=l=>(item.stagesDetailed||[]).filter(s=>{const t=`${s.title||''} ${(s.items||[]).join(' ')}`;if(l==='بكالوريوس'||l==='ماجستير')return !/ما بعد الدكتوراه فقط|الدكتوراه وما بعد الدكتوراه فقط/.test(t);if(l==='دكتوراه')return !/ما بعد الدكتوراه فقط/.test(t);return true});const renderLevels=()=>levels.map((l,i)=>`<button class="level-tab${i===0?' active':''}" data-level="${esc(l)}">${esc(l)}</button>`).join('');const renderPanel=l=>{const elig=levelSpecific(item.eligibility,l);const stages=stageFilter(l);const docs=levelSpecific(item.documents,l);return `<div class="level-panel"><h3>من يستطيع التقديم؟</h3><ul class="clean-list">${(elig.length?elig:item.eligibility||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><h3>مراحل التقديم</h3>${stages.map((s,i)=>`<article class="stage${i===0?' open':''}"><button type="button"><span>${esc(s.title||`المرحلة ${i+1}`)}</span><span>${esc(s.date||'')}</span></button><div class="stage-body"><ul class="clean-list">${(s.items||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></article>`).join('')}<h3>المستندات المطلوبة</h3><ul class="clean-list">${(docs.length?docs:item.documents||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`};root.innerHTML=`<section class="new-hero"><div class="new-badges"><span class="new-badge">${esc(item.country||'دولي')}</span><span class="new-badge">${esc(item.funding||'حسب البرنامج')}</span><span class="new-badge">${esc(item.statusLabel||'راجع الحالة')}</span></div><h1>${esc(item.title)}</h1><p>${esc(item.shortDescription||'')}</p><div class="new-provider"><small>الجهة</small><b>${esc(item.provider||'—')}</b></div></section>${item.facts?.length?`<section class="facts">${item.facts.map(f=>`<div class="fact"><small>${esc(f[0])}</small><b>${esc(f[1])}</b></div>`).join('')}</section>`:''}<section class="new-box"><h2>اختر مرحلتك الدراسية</h2><div class="level-tabs">${renderLevels()}</div><div id="level-content">${renderPanel(levels[0]||'جميع المراحل')}</div></section>${item.subjectAreas?.length?`<section class="new-box"><h2>التخصصات والمجالات</h2><div class="chip-grid">${item.subjectAreas.map(x=>`<span class="chip">${esc(x)}</span>`).join('')}</div></section>`:''}${item.portfolio?.length?`<section class="new-box"><h2>البورتفوليو وما تحتاج لتجهيزه</h2><ul class="clean-list">${item.portfolio.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`:''}${item.coverage?.length?`<section class="new-box"><h2>ماذا يغطي التمويل؟</h2><ul class="clean-list">${item.coverage.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`:''}${item.notes?.length?`<section class="new-box"><h2>ملاحظات مهمة قبل التقديم</h2><ul class="clean-list">${item.notes.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`:''}<section class="new-box"><h2>المصدر والتقديم</h2><div class="actions">${item.officialUrl?`<a class="btn outline" target="_blank" rel="noopener" href="${esc(item.officialUrl)}">المصدر الرسمي</a>`:''}${item.applyUrl?`<a class="btn primary" target="_blank" rel="noopener" href="${esc(item.applyUrl)}">التقديم الرسمي</a>`:''}<a class="btn outline" href="scholarships.html">العودة للمنح</a></div></section>`;root.querySelectorAll('.level-tab').forEach(btn=>btn.onclick=()=>{root.querySelectorAll('.level-tab').forEach(x=>x.classList.remove('active'));btn.classList.add('active');document.getElementById('level-content').innerHTML=renderPanel(btn.dataset.level);bindStages()});function bindStages(){root.querySelectorAll('.stage button').forEach(btn=>btn.onclick=()=>btn.parentElement.classList.toggle('open'))}bindStages();}
