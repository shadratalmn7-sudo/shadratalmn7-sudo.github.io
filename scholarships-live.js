import { getApp, getApps, initializeApp } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-app.js';
import { collection, getDocs, getFirestore, query, where } from 'https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js';
import { firebaseConfig } from './firebase-config.js';
import { mergeScholarships } from './scholarship-catalog.js';
if(location.pathname.endsWith('/scholarships.html')){
 const app=getApps().length?getApp():initializeApp(firebaseConfig),db=getFirestore(app),cards=document.querySelector('#scholarship-cards'),count=document.querySelector('#scholarship-count'),empty=document.querySelector('#scholarship-empty');
 const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const icon=c=>({'روسيا':'🇷🇺','المجر':'🇭🇺','تركيا':'🇹🇷'}[c]||'🌍');
 const date=v=>{if(!v)return null;const d=new Date(String(v).length===10?`${v}T00:00:00`:v);return isNaN(d)?null:d};
 const prettyDate=v=>{const d=date(v);return d?d.toLocaleDateString('ar-SA',{year:'numeric',month:'long',day:'numeric'}):'بانتظار الإعلان'};
 const timer=s=>{const now=Date.now(),o=date(s.openDate||s.openingDate),c=date(s.deadline||s.closeDate||s.endDate);let t,l;if(o&&now<o){t=o;l='يفتح التقديم خلال'}else if(c&&now<c){t=c;l='ينتهي التقديم خلال'}else if(c&&now>=c)return{l:'حالة التقديم',v:'مغلق حاليًا'};else return{l:'موعد التقديم',v:s.statusLabel||'بانتظار الإعلان'};const total=Math.max(0,t-now),days=Math.floor(total/86400000),hours=Math.floor((total%86400000)/3600000),mins=Math.floor((total%3600000)/60000);let v;if(days>0)v=`${days} يوم${hours?` و ${hours} ساعة`:''}`;else if(hours>0)v=`${hours} ساعة${mins?` و ${mins} دقيقة`:''}`;else v=`${Math.max(1,mins)} دقيقة`;return{l,v}};
 let remote=[];try{const snap=await getDocs(query(collection(db,'scholarships'),where('publishStatus','==','published')));remote=snap.docs.map(d=>({id:d.id,...d.data()}))}catch(e){console.warn(e)}const all=mergeScholarships(remote).filter(x=>x.publishStatus==='published').sort((a,b)=>(a.sortOrder??999)-(b.sortOrder??999));let active='all';
 function render(){const items=active==='all'?all:all.filter(s=>s.country===active);if(count)count.textContent=`${items.length} منحة`;if(empty)empty.hidden=!!items.length;if(!cards)return;cards.innerHTML=items.map(s=>{const slug=s.slug||s.id,t=timer(s),levels=(s.studyLevels||[]).slice(0,4).join(' • '),stageCount=(s.stagesDetailed||[]).length;return `<article class="scholarship-card scholarship-${s.country==='روسيا'?'ru':s.country==='المجر'?'hu':s.country==='تركيا'?'tr':'global'}"><div class="scholarship-accent"></div><div class="scholarship-card-top"><span class="country-chip">${icon(s.country)} ${esc(s.country||'دولي')}</span><span class="status-chip">${esc(s.statusLabel||'منحة')}</span></div><div class="scholarship-provider">${esc(s.provider||'جهة مانحة')}</div><h2>${esc(s.title)}</h2><p class="scholarship-summary">${esc(s.shortDescription||'')}</p><div class="scholarship-countdown"><small>${esc(t.l)}</small><strong>${esc(t.v)}</strong><span>تاريخ البداية: ${esc(prettyDate(s.openDate||s.openingDate))}</span></div><div class="scholarship-meta-row"><div><small>التمويل</small><b>${esc(s.funding||'حسب البرنامج')}</b></div><div><small>المراحل الدراسية</small><b>${esc(levels||'حسب البرنامج')}</b></div><div><small>مراحل التقديم</small><b>${stageCount?`${stageCount} مراحل مفصلة`:'تظهر داخل الدليل'}</b></div></div><a class="btn primary scholarship-details-btn" href="scholarship.html?slug=${encodeURIComponent(slug)}">عرض تفاصيل المنحة</a></article>`}).join('')}
 document.querySelectorAll('button.country-filter[data-country]').forEach(btn=>btn.onclick=e=>{e.preventDefault();e.stopPropagation();active=btn.dataset.country||'all';document.querySelectorAll('button.country-filter').forEach(x=>x.classList.toggle('is-active',x===btn));render();return false});render();setInterval(render,60000);
}
