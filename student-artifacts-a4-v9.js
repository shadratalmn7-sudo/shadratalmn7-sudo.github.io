import * as base from './student-artifacts.js?v=4';

export const auth=base.auth;
export const db=base.db;
export const safeFilename=base.safeFilename;
export const waitForUser=base.waitForUser;
export const saveArtifact=base.saveArtifact;
export const logStudentActivity=base.logStudentActivity;
export const listUserSavedItems=base.listUserSavedItems;
export const listArtifacts=base.listArtifacts;
export const listActivity=base.listActivity;
export const getArtifact=base.getArtifact;
export const deleteArtifact=base.deleteArtifact;
export const timestampMs=base.timestampMs;
export const formatSavedAt=base.formatSavedAt;

const PAGE_W=794,PAGE_H=1123;
const nextPaint=()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));
const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));
const absoluteUrl=src=>{try{return new URL(src,document.baseURI).href}catch{return src}};
const isIOS=()=>/iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);
const nodeDirection=node=>(node?.dir||getComputedStyle(node).direction||'ltr').toLowerCase()==='rtl'?'rtl':'ltr';

const ensureCssLink=(href,key)=>{if(document.querySelector(`link[data-${key}]`))return;const link=document.createElement('link');link.rel='stylesheet';link.href=href;link.dataset[key]='1';document.head.appendChild(link)};
export function ensureBuilderCss(){ensureCssLink('builder-system.css?v=3','builderSystem');ensureCssLink('builder-a4.css?v=4','builderA4')}

const loadExternalScript=(src,test,timeout=18000)=>new Promise((resolve,reject)=>{
  if(test?.())return resolve(true);const expected=absoluteUrl(src),old=[...document.scripts].find(s=>s.src===expected);
  if(old?.dataset.shadratLoaded==='1'){if(test?.())return resolve(true);old.remove()}
  const script=document.createElement('script');script.src=src;script.async=true;script.crossOrigin='anonymous';
  const timer=setTimeout(()=>{script.remove();reject(new Error(`SCRIPT_TIMEOUT:${src}`))},timeout);
  script.onload=()=>{clearTimeout(timer);script.dataset.shadratLoaded='1';test?.()?resolve(true):reject(new Error(`SCRIPT_GLOBAL_MISSING:${src}`))};
  script.onerror=()=>{clearTimeout(timer);script.remove();reject(new Error(`SCRIPT_LOAD_FAILED:${src}`))};document.head.appendChild(script)
});
async function loadFromMirrors(urls,test){if(test?.())return true;let last=null;for(const src of urls){try{await loadExternalScript(src,test);if(test?.())return true}catch(error){last=error;console.warn('[Shadrat] PDF library mirror failed',src,error)}}throw last||new Error('PDF_LIBRARY_UNAVAILABLE')}
async function ensureHtml2Canvas(){if(window.html2canvas)return;await loadFromMirrors(['https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js','https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js','https://unpkg.com/html2canvas@1.4.1/dist/html2canvas.min.js'],()=>!!window.html2canvas)}
async function ensureJsPdf(){if(window.jspdf?.jsPDF||window.jsPDF)return;await loadFromMirrors(['https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js','https://cdn.jsdelivr.net/npm/jspdf@2.5.1/dist/jspdf.umd.min.js','https://unpkg.com/jspdf@2.5.1/dist/jspdf.umd.min.js'],()=>!!(window.jspdf?.jsPDF||window.jsPDF))}

const sanitizeStoredHtml=(html='')=>{const parsed=new DOMParser().parseFromString(String(html),'text/html');parsed.querySelectorAll('script,style,iframe,object,embed,link,meta,base').forEach(el=>el.remove());parsed.querySelectorAll('*').forEach(el=>[...el.attributes].forEach(attr=>{const name=attr.name.toLowerCase(),value=attr.value.trim().toLowerCase();if(name.startsWith('on')||name==='srcdoc'||((name==='href'||name==='src')&&value.startsWith('javascript:')))el.removeAttribute(attr.name)}));return parsed.body.innerHTML};
function normalizeRoot(clone,multiPage=false){const props={position:'relative',top:'auto',left:'auto',right:'auto',bottom:'auto',inset:'auto',transform:'none','transform-origin':'initial',visibility:'visible',opacity:'1','box-shadow':'none','border-radius':'0',margin:'0',width:`${PAGE_W}px`,'min-width':`${PAGE_W}px`,'max-width':`${PAGE_W}px`,height:multiPage?'auto':`${PAGE_H}px`,'min-height':`${PAGE_H}px`,'max-height':multiPage?'none':`${PAGE_H}px`,'box-sizing':'border-box',overflow:multiPage?'visible':'hidden'};Object.entries(props).forEach(([k,v])=>clone.style.setProperty(k,v,'important'));return clone}
function strengthenBidi(root,dir){root.setAttribute('dir',dir);root.style.setProperty('direction',dir,'important');root.style.setProperty('unicode-bidi','isolate','important');root.querySelectorAll('h1,h2,h3,p,li,.role,.contact,.letter-meta,.letter-body,.letter-body p').forEach(el=>{el.setAttribute('dir',dir);el.style.setProperty('direction',dir,'important');el.style.setProperty('unicode-bidi','plaintext','important');el.style.setProperty('letter-spacing','0','important')})}

async function renderHtml2Canvas(node,scale,nativeText=false){
  await ensureHtml2Canvas();const multiPage=node.dataset.multipage==='true',clone=normalizeRoot(node.cloneNode(true),multiPage),dir=nodeDirection(node);strengthenBidi(clone,dir);
  const shell=document.createElement('div');shell.className='artifact-pdf-shell';shell.dir=dir;shell.style.cssText=`position:fixed;top:0;left:-10000px;width:${PAGE_W}px;height:auto;min-height:${PAGE_H}px;margin:0;padding:0;background:#fff;z-index:2147483645;overflow:visible;box-sizing:border-box;pointer-events:none;direction:${dir};`;shell.appendChild(clone);document.body.appendChild(shell);
  try{await nextPaint();await wait(80);const contentHeight=multiPage?Math.max(PAGE_H,clone.scrollHeight,clone.offsetHeight):PAGE_H;return await window.html2canvas(clone,{scale,useCORS:true,allowTaint:false,backgroundColor:'#ffffff',logging:false,removeContainer:true,foreignObjectRendering:nativeText,scrollX:0,scrollY:0,width:PAGE_W,height:contentHeight,windowWidth:PAGE_W,windowHeight:contentHeight,onclone:doc=>{const root=doc.querySelector('.cv,.letter');if(root)strengthenBidi(root,dir)}})}finally{shell.remove()}
}
function safePageSlices(canvas,maxSlice){const slices=[];let offset=0,ctx=null;try{ctx=canvas.getContext('2d',{willReadFrequently:true})}catch{}const scanBack=Math.max(42,Math.round(maxSlice*.16));while(offset<canvas.height){const remaining=canvas.height-offset;if(remaining<=maxSlice){slices.push({offset,height:remaining});break}const target=Math.min(canvas.height,offset+maxSlice);let bestY=target;if(ctx){const start=Math.max(offset+Math.round(maxSlice*.74),target-scanBack),height=Math.max(1,target-start);try{const pixels=ctx.getImageData(0,start,canvas.width,height).data;let bestScore=Infinity;for(let y=0;y<height;y+=2){let dark=0;for(let x=Math.round(canvas.width*.06);x<canvas.width*.94;x+=10){const i=(y*canvas.width+x)*4;if(pixels[i]<238||pixels[i+1]<238||pixels[i+2]<238)dark++}const score=dark+(height-y)/height*.8;if(score<bestScore){bestScore=score;bestY=start+y}}}catch{bestY=target}}if(bestY<=offset+Math.round(maxSlice*.58))bestY=target;slices.push({offset,height:Math.max(1,bestY-offset)});offset=bestY}return slices}
function templateTheme(node){const c=node?.classList||{contains:()=>false};if(c.contains('template-modern')||c.contains('letter-template-modern'))return['#2563eb',false];if(c.contains('template-sidebar'))return['#2563eb',true];if(c.contains('template-technical')||c.contains('letter-template-technical'))return['#0f172a',false];if(c.contains('template-creative')||c.contains('letter-template-creative'))return['#7c3aed',false];if(c.contains('template-executive')||c.contains('letter-template-executive'))return['#334155',true];if(c.contains('template-academic')||c.contains('letter-template-academic'))return['#1e3a8a',false];if(c.contains('template-timeline'))return['#0f766e',true];if(c.contains('template-editorial')||c.contains('letter-template-editorial'))return['#111827',false];if(c.contains('template-formal')||c.contains('letter-template-formal'))return['#334155',false];if(c.contains('letter-template-serif'))return['#8b5e3c',false];if(c.contains('letter-template-clean'))return['#475569',true];if(c.contains('letter-template-navy'))return['#0f2f57',true];return['#64748b',false]}
function decoratePdfPage(ctx,canvas,node,pageNumber,totalPages){const[accent,side]=templateTheme(node),w=canvas.width,h=canvas.height,unit=w/PAGE_W;ctx.save();ctx.fillStyle=accent;if(side){const sw=Math.max(8,8*unit),rtl=nodeDirection(node)==='rtl';ctx.fillRect(rtl?w-sw:0,0,sw,h)}else ctx.fillRect(0,0,w,Math.max(6,7*unit));ctx.strokeStyle='rgba(15,23,42,.18)';ctx.lineWidth=Math.max(1,unit);ctx.strokeRect(5*unit,5*unit,w-10*unit,h-10*unit);ctx.fillStyle='#64748b';ctx.font=`${Math.max(10,10*unit)}px Arial,Tahoma,sans-serif`;ctx.textAlign='center';ctx.direction='ltr';ctx.fillText(`${pageNumber} / ${totalPages}`,w/2,h-12*unit);ctx.restore()}
async function canvasToDataUrl(canvas){try{return canvas.toDataURL('image/jpeg',0.95)}catch{}if(typeof canvas.toBlob==='function'){const blob=await new Promise((resolve,reject)=>canvas.toBlob(v=>v?resolve(v):reject(new Error('CANVAS_BLOB_EMPTY')),'image/jpeg',0.95));return await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=()=>reject(r.error||new Error('FILE_READER_FAILED'));r.readAsDataURL(blob)})}throw new Error('CANVAS_EXPORT_UNAVAILABLE')}
async function canvasToPdfBlob(canvas,node){await ensureJsPdf();const JsPDF=window.jspdf?.jsPDF||window.jsPDF;if(!JsPDF)throw new Error('JSPDF_MISSING');const pdf=new JsPDF({orientation:'portrait',unit:'mm',format:'a4',compress:true,putOnlyUsedFonts:true}),pagePixels=Math.max(1,Math.round(canvas.width*PAGE_H/PAGE_W)),slices=safePageSlices(canvas,pagePixels),total=slices.length;for(let i=0;i<total;i++){const{offset,height}=slices[i],page=document.createElement('canvas');page.width=canvas.width;page.height=pagePixels;const ctx=page.getContext('2d',{alpha:false});if(!ctx)throw new Error('PAGE_CANVAS_CONTEXT_MISSING');ctx.fillStyle='#fff';ctx.fillRect(0,0,page.width,page.height);ctx.drawImage(canvas,0,offset,canvas.width,height,0,0,canvas.width,height);decoratePdfPage(ctx,page,node,i+1,total);if(i)pdf.addPage();pdf.addImage(await canvasToDataUrl(page),'JPEG',0,0,210,297,undefined,'FAST')}const blob=pdf.output('blob');if(!blob?.size)throw new Error('PDF_BLOB_EMPTY');return blob}
function saveBlob(blob,filename){const name=safeFilename(filename,'Shadrat-file.pdf');if(navigator.msSaveOrOpenBlob){navigator.msSaveOrOpenBlob(blob,name);return}const url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=name;link.rel='noopener';link.style.display='none';document.body.appendChild(link);try{link.click()}finally{link.remove();setTimeout(()=>URL.revokeObjectURL(url),90000)}}
function busyOverlay(){const el=document.createElement('div');el.setAttribute('role','status');el.setAttribute('aria-live','polite');el.textContent='جاري تجهيز ملف PDF…';el.style.cssText='position:fixed;inset:0;z-index:2147483646;display:grid;place-items:center;background:rgba(248,251,255,.985);color:#1d4ed8;font:800 16px Tahoma,Arial,sans-serif;text-align:center;padding:24px;';document.body.appendChild(el);return el}
async function makePdfBlob(node){if(document.fonts?.ready)await document.fonts.ready.catch(()=>{});await nextPaint();const memory=Number(navigator.deviceMemory||0),scale=isIOS()||(memory>0&&memory<=4)?1.3:1.55,rtl=nodeDirection(node)==='rtl';let canvas;if(rtl){try{canvas=await renderHtml2Canvas(node,scale,true)}catch(error){console.warn('[Shadrat] RTL native-text capture failed; using canvas fallback',error);canvas=await renderHtml2Canvas(node,scale,false)}}else{try{canvas=await renderHtml2Canvas(node,scale,false)}catch(error){console.warn('[Shadrat] canvas capture failed; using native-text fallback',error);canvas=await renderHtml2Canvas(node,scale,true)}}if(!canvas?.width||!canvas?.height)throw new Error('PDF_CANVAS_EMPTY');return canvasToPdfBlob(canvas,node)}

export async function downloadNodePdf(node,filename){if(!node)throw new Error('PDF_NODE_MISSING');ensureBuilderCss();const overlay=busyOverlay(),oldOverflow=document.documentElement.style.overflow;document.documentElement.style.overflow='hidden';try{const blob=await makePdfBlob(node);saveBlob(blob,filename);return true}finally{overlay.remove();document.documentElement.style.overflow=oldOverflow}}
export async function downloadArtifactPdf(artifact){ensureBuilderCss();const holder=document.createElement('div');if(artifact.renderedHtml)holder.innerHTML=sanitizeStoredHtml(artifact.renderedHtml);else{const el=document.createElement('article');el.className=artifact.artifactType==='motivation'?'letter letter-template-academic':'cv template-modern';const p=document.createElement('p');p.textContent=artifact.renderedText||'';el.appendChild(p);holder.appendChild(el)}const node=holder.firstElementChild;if(!node)throw new Error('ARTIFACT_RENDER_EMPTY');if(artifact.language==='ar'||/[؀-ۿ]/.test(node.textContent||''))node.setAttribute('dir','rtl');node.dataset.multipage='true';const suffix=artifact.artifactType==='motivation'?'Motivation-Letter':'CV';return downloadNodePdf(node,`${safeFilename(artifact.studentName||artifact.artifactName||'Student')}-${suffix}.pdf`)}
