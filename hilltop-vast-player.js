(function () {
  'use strict';
  // Disabled intentionally: this custom player was using a non-verified URL as a VAST ad tag,
  // so the shell appeared without a playable/countable HilltopAds video impression.
  // Replace this file only after obtaining the official HilltopAds Video Slider JS code
  // or a real Video VAST zone feed URL from the HilltopAds publisher dashboard.
})();
